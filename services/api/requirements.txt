# Human-edited inputs — lower-bound (`>=`) pins only; a regression test enforces
# that (tests/test_dependency_lock.py). The exact resolution lives in
# requirements.lock (used by setup and CI); regenerate it per
# docs/dev-workflows.md#python-dependency-updates after editing this file.
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
python-dotenv>=1.2.2
pydantic>=2.10.0
pydantic-settings>=2.7.0
boto3>=1.35.0
Pillow>=11.0.0
PyPDF2>=3.0.0
# Point-cloud parsing + BEV previews for the base app (KITTI .bin frames,
# frame stats). numpy is pinned < 2 because the opt-in MMDetection3D engine
# group (requirements-engine.txt) needs numpy<2, and the base arrays must stay
# ABI-compatible with it.
numpy>=1.26.0,<2.0.0

# Dev / quality
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
